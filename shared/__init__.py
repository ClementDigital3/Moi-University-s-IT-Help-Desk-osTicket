"""Shared layer for the ICT help desk ticket models.

Every model in models/ draws its data, representation, context blocks,
candidate classifier heads and evaluation metrics from here, so that the
models differ only in what they actually model -- not in how they load a
split or compute an F1.
"""
